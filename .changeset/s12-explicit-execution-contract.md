---
"@tech-club/benchmark": patch
---

Add an explicit prospective S12 execution contract that separates subject attempts from model turns, enforces its cumulative attempt deadline, and preserves legacy configuration semantics.
