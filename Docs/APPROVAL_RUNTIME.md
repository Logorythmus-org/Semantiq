# Approval Runtime

Approval is required before deleting knowledge, publishing, running external providers, executing dangerous tools, repository writes, workflow changes, agent installation, permission changes, and policy changes.

Approval history is append-only and immutable by contract.
