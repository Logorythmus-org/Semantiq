# Compliance Dashboard

The local dashboard exposes AI registry, provider usage, approval coverage, data export/deletion tools, consent records, telemetry status, federation sharing records, plugin permissions, agent permissions, warnings and known limitations.
