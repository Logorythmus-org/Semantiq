# Duplicate Detection

Implemented strategies: normalized text, keyword overlap, tag overlap, graph-neighborhood placeholder, and human-review action recommendations.

Questions are never deleted automatically.
