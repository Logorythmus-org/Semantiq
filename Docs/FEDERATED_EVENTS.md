# Federated Events

Only approved event types may leave a node. Forwarding includes filtering, transformation, schema validation, signatures, receipts, retry, dead-letter metadata, replay protection, sequencing, policy enforcement, and versioning.
