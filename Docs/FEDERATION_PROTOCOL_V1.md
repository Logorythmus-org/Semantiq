# Federation Protocol v1

Protocol v1 uses transport-independent envelopes for metadata, negotiation, capability discovery, auth challenge, trust/policy exchange, knowledge query, object request/response, replication, events, sync, conflicts, health, and revocation.
