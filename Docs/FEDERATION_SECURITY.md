# Federation Security

Implemented locally: signed envelope metadata, nonce, expiration, gateway validation, policy enforcement, approval gates, revocation, audit events, allowlist descriptors, size/rate-limit descriptors, and secure invitation tokens.
