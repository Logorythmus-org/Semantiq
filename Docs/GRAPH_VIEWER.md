# Graph Viewer

The Sprint 1 graph viewer returns nodes, edges, selected state, zoom, pan, filters, timeline mode, and layout metadata.

Question-to-knowledge relations are persisted in the local graph runtime and exposed in dashboard summaries and export snapshots.
