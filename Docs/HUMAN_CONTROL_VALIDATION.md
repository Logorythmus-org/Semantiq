# Human Control Validation

Validate reject, edit, restore original text, cancel workflows, pause agents, deny external calls, deny tool use, inspect logs, understand approvals, revoke federation sharing, disable plugins and enter Safe Mode.
