# Identity Runtime

Sprint 1 implements local identity records with profile, optional avatar, language, timezone, preferences, roles, workspace memberships, settings, history IDs, audit IDs, anonymous/offline support, and a semantic wallet stub.

Authentication supports local login, device auth, remembered encrypted sessions, and logout. OAuth, recovery, and federation are declared as adapter stubs for later hosted and distributed identity work.
