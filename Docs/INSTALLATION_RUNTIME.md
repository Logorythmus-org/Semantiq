# Installation Runtime

Installation plan flow: inspect manifest, check compatibility, resolve dependencies, review permissions, verify integrity, run security validation, request approval, install in sandbox, health check, register capabilities, and audit.
