# Knowledge Object

Knowledge Objects are reusable semantic primitives. Each object has identity, workspace, owner, kind, title, metadata, relations, version, tags, history, Semantiq reports, permissions, comments, attachments, timeline links, and graph links.

Questions, evidence, hypotheses, documents, workflows, datasets, narratives, and future objects should build on this model instead of redefining base fields.
