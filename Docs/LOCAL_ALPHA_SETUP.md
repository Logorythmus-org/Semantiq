# Local Alpha Setup

Run local install, validate with `techclub alpha validate`, keep telemetry disabled unless consented, create a workspace, run onboarding and create a backup before testing federation.
