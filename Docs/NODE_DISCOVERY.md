# Node Discovery

Discovery supports manual endpoint, invitation link, QR, LAN, known-node directory, organization adapter, offline package, and test fixture modes. Public crawling is disabled by default.
