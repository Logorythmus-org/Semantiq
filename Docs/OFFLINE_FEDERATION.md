# Offline Federation

Offline packages support invitations, node metadata, agreement drafts, references, graph snapshots, event batches, sync checkpoints, integrity manifests, signatures, and import instructions.
