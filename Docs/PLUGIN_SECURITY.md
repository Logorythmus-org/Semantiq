# Plugin Security

Plugins use capability APIs, permission prompts, filesystem isolation descriptors, deny-by-default network policy, workspace scope, timeout, memory limit, process isolation adapter, audit logging, disable switch, and health monitoring.

Unsupported: strong OS-level isolation guarantees in the current local descriptor runtime.
