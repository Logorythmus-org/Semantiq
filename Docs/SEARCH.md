# Search

Sprint 1 local search scans workspace names, knowledge titles, knowledge bodies, tags, question text, question tags, and activity events.

Search emits `SearchExecuted` and returns scored results with explanations for dashboard and command-palette use.
