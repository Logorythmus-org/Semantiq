# Security And Privacy

Sprint 2 defaults to offline local analysis. External providers are disabled unless configured and approved. Private workspace content must not leave the device silently.

Implemented as contracts/descriptors: workspace isolation, provider permission policy, redaction, prompt injection safeguards, output validation, audit logs, rate-limit policy, safe retry policy, encrypted credential requirement, and deletable/exportable AI history.
