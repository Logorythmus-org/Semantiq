# Two Node Federation Alpha

Use invitation-only federation. Create both node identities, exchange invitation, approve trust, negotiate policy, share one synthetic question, test revocation and export the federation audit.
