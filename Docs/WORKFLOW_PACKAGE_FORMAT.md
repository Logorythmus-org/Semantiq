# Workflow Package Format

Workflow packages declare nodes, edges, required agents/tools, inputs, outputs, variables, approval gates, failure paths, retry policy, Semantiq profile, tests, examples, and docs.
