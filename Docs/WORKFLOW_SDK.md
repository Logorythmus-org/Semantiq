# Workflow SDK

The Workflow SDK lets developers extend workflow creation and execution.

## Extensions
Workflow templates, workflow nodes, workflow validators, workflow actions, workflow simulators, workflow optimizers, and workflow extensions.

## Rules
Workflow extensions must declare permissions, validation behavior, simulation behavior, retry behavior, and approval requirements.
