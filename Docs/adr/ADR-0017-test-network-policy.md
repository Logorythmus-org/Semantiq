# ADR-0017: Test Network Policy

Normal tests do not initialize remote providers and local AI is disabled by default. The repository does not claim OS-level network blocking. Any future network test must be an explicit opt-in suite with isolated credentials.
