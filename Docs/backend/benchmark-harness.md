# Benchmark Harness

Benchmark settings are provided by the benchmark profile. Benchmark execution is not part of `pnpm test` or `pnpm verify`; it requires an explicit benchmark command and stable local datasets.
