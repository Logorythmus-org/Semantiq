# Coverage Policy

V8 reports statements, branches, functions, and lines for config, shared, and persistence. The baseline is measured and recorded; no arbitrary threshold is enforced until API and real database paths exist.
