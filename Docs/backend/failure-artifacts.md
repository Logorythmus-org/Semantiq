# Failure Artifacts

`pnpm verify` writes stage output and status to `artifacts/verification/summary.json`. Coverage reports are written to `artifacts/coverage`. These paths are ignored by repository hygiene rules.
