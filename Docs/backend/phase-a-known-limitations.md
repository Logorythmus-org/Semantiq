# Phase A Known Limitations

The API Docker image is intentionally minimal and does not include the full test toolchain. Docker health checks database port availability rather than SQL authentication. Persistent idempotency adapter/concurrency integration and full in-container tests remain follow-up work. Two historical lint warnings remain.
