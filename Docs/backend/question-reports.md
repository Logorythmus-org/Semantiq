# Question Reports

Reports are private user signals, not verdicts. States are `open`, `under_review`, `resolved`, `dismissed`, and `withdrawn`. Submission does not change Question or moderation state.

Only a reporter may withdraw an open report. Moderators may read reports through the narrow capability contract.
