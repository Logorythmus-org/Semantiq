# Question Sources

Supported types are `web`, `book`, `paper`, `dataset`, `document`, `conversation`, `observation`, `experiment`, `personal_experience`, `repository`, and `other`.

HTTP(S), DOI-like, and ISBN-like locators receive structural validation. Other bounded locators are NFKC-normalized and case-normalized. No remote content is fetched.
