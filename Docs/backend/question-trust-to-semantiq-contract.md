# Question Trust to Semantiq Contract

Semantiq may consume provenance classifications, public trust facts, lifecycle state, Frame freshness, and stable events. It must preserve classifications and may not reinterpret a report or moderation state as truth.

Internal report counts and actor identities require separate authorization and are not part of the public Semantiq contract.
