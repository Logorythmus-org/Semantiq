# Shared Workflow Context

Status: Unavailable

No minimized immutable context version exists. Legacy shared-memory and context arrays are excluded. Future context must use approved versioned references and preserve access policy, fingerprints, and tamper evidence.
