# Test Database

PostgreSQL is the authoritative integration database. Test URLs must clearly target a test database. Current local tests use fake SQL clients because Docker is unavailable; real migration and repository integration is a Prompt 7 prerequisite.
