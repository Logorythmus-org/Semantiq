# Test Network Policy

Normal tests do not call remote services. Local AI and optional providers are disabled unless explicitly configured. This is a code-level default-deny policy, not an OS network sandbox; no full network-blocking claim is made.
