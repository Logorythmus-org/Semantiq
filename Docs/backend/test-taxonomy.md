# Test Taxonomy

`tests/unit` has no external services; `tests/integration` composes local modules; `tests/contracts` protects shared interfaces; `tests/security` protects redaction and boundaries; `tests/smoke` checks repository health; package `tests` protect existing runtime behavior. Benchmarks and Docker checks are explicit.
