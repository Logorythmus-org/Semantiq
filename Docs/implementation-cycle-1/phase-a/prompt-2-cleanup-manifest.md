# Prompt 2 Cleanup Manifest

Scope: Implementation Cycle 1, Phase A, Prompt 2.  
Mode: local-only cleanup and normalization. No product runtime features were added.

| Change                                      | Risk   | Reason                                                                                   | Affected Paths                                                      | Rollback                          | Validation                                              |
| ------------------------------------------- | ------ | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------- |
| Normalize generated-file ignores            | Low    | Prevent local build/cache/log artifacts from being treated as source                     | `.gitignore`, `.dockerignore`                                       | Restore previous ignore files     | `pnpm test`, `docker compose config --quiet`            |
| Approve pnpm build script for esbuild       | Low    | Required for deterministic local install                                                 | `pnpm-workspace.yaml`                                               | Remove `allowBuilds.esbuild`      | `pnpm install --frozen-lockfile`                        |
| Make Husky prepare safe without Git         | Low    | Workspace is not currently a Git repository                                              | `package.json`, `scripts/prepare.mjs`                               | Restore `prepare: husky`          | `pnpm install --frozen-lockfile`                        |
| Add repository health script                | Low    | Safe local status check without modifying user data                                      | `scripts/repository-health.mjs`, `package.json`                     | Remove script and package command | `pnpm health`                                           |
| Add typed local configuration layer         | Medium | Establish one authoritative config loading path                                          | `packages/config/src/index.ts`, `.env.example`, `.env.test.example` | Restore previous config scaffold  | `pnpm typecheck`, `pnpm test`                           |
| Add shared logging and error primitives     | Medium | Establish minimal consistent backend logging/error baseline                              | `packages/shared/src/index.ts`                                      | Restore previous shared scaffold  | `pnpm typecheck`, `pnpm test`                           |
| Deprecate duplicate workflow-runtime source | Low    | Remove exact duplicate implementation while preserving import compatibility              | `packages/workflow-runtime/src/index.ts`                            | Restore duplicate source          | duplicate scan, `pnpm typecheck`                        |
| Normalize lint baseline                     | Low    | Keep lint meaningful for current scaffold without rewriting feature runtimes             | `eslint.config.mjs`                                                 | Restore previous lint config      | `pnpm lint`                                             |
| Add cleanup-critical tests                  | Low    | Validate config failure modes, logging redaction, error serialization, and health script | `tests/unit/*`, `tests/integration/*`, `tests/smoke/*`              | Remove new tests                  | `pnpm test`, `pnpm test:integration`, `pnpm test:smoke` |
| Add cleanup documentation                   | Low    | Required sprint artifacts and future handoff                                             | `Docs/implementation-cycle-1/**`, `Docs/adr/**`                     | Remove added docs                 | `pnpm format:check`                                     |

No broad deletion was performed. Generated `.turbo` logs remain in the local working tree but are now ignored.
