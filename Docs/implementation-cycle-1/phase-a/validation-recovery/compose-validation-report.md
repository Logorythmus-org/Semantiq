# Compose Validation Report

`docker compose config --quiet`, services, volumes, and networks passed. Recovery added a real `api` service, PostgreSQL healthcheck, API healthcheck, and health-based dependency ordering. Validation project name `techclub-validation` created only `techclub-validation_postgres-data` and its network.
