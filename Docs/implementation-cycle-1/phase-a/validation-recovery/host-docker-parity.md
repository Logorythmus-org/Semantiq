# Host/Docker Parity

Passed for configuration-independent infrastructure behavior: API response envelope, correlation IDs, health/readiness semantics, disabled AI state, and PostgreSQL port availability matched. Expected differences are Docker hostnames (`postgres`/`host.docker.internal`), bind address, and filesystem roots. Full application behavior remains limited to the Phase A API foundation.
