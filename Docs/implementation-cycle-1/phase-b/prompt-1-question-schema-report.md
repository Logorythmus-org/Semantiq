# Phase B Prompt 1 Question Schema Report

Migration 2, `questions`, is additive after Phase A migration 1. It creates `questions` with text length check 10–2000, published status check, language format check, source allow-list, nullable creator, UTC timestamps, version check, primary key, and created-at index. No existing table or data was modified. Migration head is `2/questions`.
