export const conflictScreens = ["Conflict Resolution Center"] as const;
export const conflictControls = ["Keep local", "Keep remote", "Merge automatically", "Merge manually", "Fork", "Preserve both", "Convert to reference", "Defer", "Reject remote update"] as const;
