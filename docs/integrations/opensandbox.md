# OpenSandbox Integration

SemantIQ provides an optional integration with the OpenSandbox daemon for isolated micro-environment execution:
- **Package**: `@semantiq/adapter-opensandbox`
- **Contract**: `@semantiq/sandbox-contracts`
- **Lifecycle**: Create sandbox → Execute command → Stream events → Destroy sandbox
