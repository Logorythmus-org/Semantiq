# Civilization Kernel Package

This package contains the contract scaffold for the Civilization Coordination Kernel. The package is intentionally adapter-oriented: it coordinates existing mature subsystems through stable interfaces instead of rebuilding them.

Each top-level directory documents a future bounded area for kernel implementation, validation, schemas, UI, analytics, and public protocol work.
