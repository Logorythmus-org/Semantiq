# Question Service

FastAPI-ready service boundary for Sprint 1 question create, edit, archive, delete, duplicate, convert, tag, bookmark, history, relation, template, and graph update endpoints.

The current implementation exports health and route descriptors for local CI. Runtime behavior is implemented in `@tech-club/sprint1-runtime`.
