# Workspace Service

FastAPI-ready service boundary for Sprint 1 workspace creation, rename, archive, restore, export, metadata, settings, templates, and timeline endpoints.

The current implementation exports health and route descriptors for local CI. Runtime behavior is implemented in `@tech-club/sprint1-runtime`.
