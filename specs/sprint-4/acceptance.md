# Acceptance

A Tech Club object can become a portable asset, validate, receive Semantiq evaluation, require approval for publication, appear in local catalog, search/install/update/rollback/export, register agent/workflow/knowledge package capability, expose license and wallet records, and pass CI.
