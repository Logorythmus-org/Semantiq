# Observability

Metrics include package build duration, validation duration/failure rate, installation success, dependency conflicts, rollback rate, plugin crashes, search latency, publication rate, review activity, Semantiq scores, security findings, registry health, SDK usage, and API errors.
