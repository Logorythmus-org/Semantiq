# Plugin Security

Plugins declare identity, version, entry point, capabilities, permissions, dependencies, configuration schema, events, commands, UI/API extensions, health, license, publisher, and integrity. Sandbox uses capability APIs, permission prompts, filesystem isolation, network policy, workspace scope, timeout, memory limits, process isolation adapter, audit logging, disable switch, and health monitoring.
