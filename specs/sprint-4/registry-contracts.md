# Registry Contracts

Registry APIs support register asset, register version, read metadata, resolve versions, resolve dependencies, list capabilities, check compatibility, deprecate, archive, verify integrity, track installation, track usage, import package, and export package.
