# Conflict Resolution

Detect concurrent edits, delete/update, relation, permission, license, schema, ownership, graph, version, and policy conflicts. Options include keep local, keep remote, merge, fork, preserve both, reference, defer, reject remote. Never discard silently.
