# Synchronization

Sync supports change detection, version vectors, incremental updates, tombstones, deletion policy, resume, retry, checkpoints, partial sync, selective fields, bandwidth-aware and low-connectivity modes, manual and background sync.
