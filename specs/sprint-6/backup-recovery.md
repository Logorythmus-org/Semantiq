# Backup Recovery

Spec ID: S6-BACKUP

## Requirements

Workspace/node/selective backups, optional encryption marker, manifest, integrity verification, restore wizard, rollback point and disaster recovery export.

## Validation

`createBackup()`, `verifyBackup()`, `restoreBackup()`.
