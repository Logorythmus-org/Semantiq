# Feedback Runtime

Spec ID: S6-FEED

## Requirements

Bug, usability, feature, AI quality, Semantiq, federation, security and docs feedback with severity, repro steps, optional diagnostics, consent, redaction, status and response history.

## Validation

`submitFeedback()` and `getFeedbackStatus()`.
