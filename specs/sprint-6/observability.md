# Observability

Spec ID: S6-OBS

## Requirements

System health exposes service, graph, federation, backup, error, security warning, consent, telemetry, feature flag, release and limitation status.

## Validation

`getSystemHealth()`.
