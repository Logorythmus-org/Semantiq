# Telemetry

Spec ID: S6-TEL

## Requirements
Telemetry is disabled by default and enabled only with consent. No private workspace content is included.

## Validation
`telemetryEnabled` default false and consent records.
